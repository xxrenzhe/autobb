# Google Ads API 混合配置方案设计

**设计日期**: 2025-11-21
**状态**: 实施中

---

## 📋 需求规格

### 配置项定义

| 参数 | 必填/选填 | 说明 |
|------|----------|------|
| **login_customer_id** | ✅ 必填 | 用户自己的MCC管理账户ID |
| client_id | ⚪ 选填 | OAuth应用Client ID |
| client_secret | ⚪ 选填 | OAuth应用Client Secret |
| developer_token | ⚪ 选填 | Google Ads API Developer Token |

### 配置规则

1. **用户未配置OAuth凭证** (client_id/client_secret/developer_token为空):
   - 共享autoads用户的OAuth凭证
   - 共享autoads用户的refresh_token
   - 使用用户自己的login_customer_id和customer_id

2. **用户已配置OAuth凭证并完成授权**:
   - 使用用户自己的OAuth凭证
   - 使用用户自己的refresh_token
   - 使用用户自己的login_customer_id和customer_id

---

## 🏗️ 架构设计

### 配置读取流程

```
┌─────────────────────────────────────────────────────────────┐
│                    getGoogleAdsConfig(userId)                │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────────┐
              │ 读取用户配置 (user_id=userId) │
              │ from system_settings          │
              └───────────────────────────────┘
                              │
                              ▼
         ┌──────────────────────────────────────────┐
         │ 用户是否配置了完整OAuth凭证?              │
         │ (client_id && client_secret &&           │
         │  developer_token 都不为空)               │
         └──────────────────────────────────────────┘
                    │                   │
              是    │                   │ 否
                    ▼                   ▼
    ┌─────────────────────┐    ┌─────────────────────────┐
    │ 使用用户自己的配置   │    │ 共享autoads用户的配置    │
    │ • client_id (用户)  │    │ • client_id (autoads)   │
    │ • client_secret     │    │ • client_secret         │
    │ • developer_token   │    │ • developer_token       │
    │ • refresh_token     │    │ • refresh_token         │
    │   (用户OAuth)       │    │   (autoads OAuth)       │
    │ • login_customer_id │    │ • login_customer_id     │
    │   (用户必填)        │    │   (用户必填)            │
    │ • customer_id (用户)│    │ • customer_id (用户)    │
    └─────────────────────┘    └─────────────────────────┘
```

### 数据存储设计

```
┌─────────────────────────────────────────────────────────────┐
│                      system_settings                         │
├─────────────────────────────────────────────────────────────┤
│ user_id=1 (autoads) - 平台共享配置                          │
│  • client_id (encrypted) ✅                                 │
│  • client_secret (encrypted) ✅                             │
│  • developer_token (encrypted) ✅                           │
│  • login_customer_id: 5010618892 ✅                         │
├─────────────────────────────────────────────────────────────┤
│ user_id=2 (tigerlion) - 用户个人配置                        │
│  • client_id (encrypted) - 可选，为空则用autoads的          │
│  • client_secret (encrypted) - 可选，为空则用autoads的      │
│  • developer_token (encrypted) - 可选，为空则用autoads的    │
│  • login_customer_id: XXXXXXXXXX ✅ 必填                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  google_ads_credentials                      │
├─────────────────────────────────────────────────────────────┤
│ user_id=1 (autoads) - 平台OAuth授权                         │
│  • refresh_token ✅                                         │
│  • access_token ✅                                          │
├─────────────────────────────────────────────────────────────┤
│ user_id=2 (tigerlion) - 用户OAuth授权（如果自己配置了凭证） │
│  • refresh_token (有则使用，无则用autoads的)                │
│  • access_token                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   google_ads_accounts                        │
├─────────────────────────────────────────────────────────────┤
│ user_id=2 (tigerlion) - 用户的广告账户                      │
│  • customer_id: YYYYYYYYYY ✅ 用户独立                      │
│  • refresh_token: NULL (不在这里存储)                       │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 实施步骤

### Step 1: 修改 settings 页面 UI

**文件**: `src/app/(app)/settings/page.tsx`

**修改内容**:
- `login_customer_id` 标记为必填 (isRequired: true)
- `client_id`, `client_secret`, `developer_token` 标记为选填 (isRequired: false)
- 添加提示信息：若不配置OAuth凭证，将共享平台配置

### Step 2: 修改配置读取逻辑

**文件**: `src/lib/keyword-planner.ts`

**修改内容**:
```typescript
function getGoogleAdsConfig(userId?: number): KeywordPlannerConfig | null {
  const db = getDatabase()
  const targetUserId = userId || 1  // 默认回退到autoads
  const autoadsUserId = 1

  // 1. 读取当前用户配置
  const userConfigs = readUserConfigs(targetUserId)

  // 2. 检查用户是否有完整OAuth凭证
  const hasFullOAuthConfig =
    userConfigs.client_id &&
    userConfigs.client_secret &&
    userConfigs.developer_token

  // 3. 根据是否有完整配置决定使用哪套凭证
  let oauthConfig: OAuthConfig

  if (hasFullOAuthConfig) {
    // 使用用户自己的配置
    oauthConfig = {
      clientId: userConfigs.client_id,
      clientSecret: userConfigs.client_secret,
      developerToken: userConfigs.developer_token,
      refreshToken: getUserRefreshToken(targetUserId)  // 用户的OAuth
    }
  } else {
    // 共享autoads的配置
    const autoadsConfigs = readUserConfigs(autoadsUserId)
    oauthConfig = {
      clientId: autoadsConfigs.client_id,
      clientSecret: autoadsConfigs.client_secret,
      developerToken: autoadsConfigs.developer_token,
      refreshToken: getUserRefreshToken(autoadsUserId)  // autoads的OAuth
    }
  }

  // 4. login_customer_id 和 customer_id 始终使用用户自己的
  return {
    ...oauthConfig,
    loginCustomerId: userConfigs.login_customer_id,  // 用户必填
    customerId: getUserCustomerId(targetUserId)       // 用户的账户
  }
}
```

### Step 3: 修改 OAuth 流程

**文件**: `src/app/api/google-ads/oauth/start/route.ts`

**修改内容**:
- 检查用户是否配置了自己的OAuth凭证
- 若有，使用用户的client_id生成授权URL
- 若无，提示用户需要先配置OAuth凭证才能独立授权

**文件**: `src/app/api/google-ads/oauth/callback/route.ts`

**修改内容**:
- 保存refresh_token到用户自己的google_ads_credentials记录

### Step 4: 更新数据库配置标记

**文件**: `scripts/update-config-requirements.ts` (新建)

**修改内容**:
```sql
-- 更新 login_customer_id 为必填
UPDATE system_settings
SET is_required = 1
WHERE category = 'google_ads' AND config_key = 'login_customer_id';

-- 更新其他三项为选填
UPDATE system_settings
SET is_required = 0
WHERE category = 'google_ads'
  AND config_key IN ('client_id', 'client_secret', 'developer_token');
```

---

## 📊 场景验证

### 场景1: 用户A未配置OAuth凭证

```
用户A配置:
  - login_customer_id: 1234567890 ✅ (必填，已配置)
  - client_id: NULL
  - client_secret: NULL
  - developer_token: NULL
  - customer_id: 从google_ads_accounts读取

实际使用:
  - client_id: autoads的 ✅
  - client_secret: autoads的 ✅
  - developer_token: autoads的 ✅
  - refresh_token: autoads的 ✅
  - login_customer_id: 用户A的 (1234567890) ✅
  - customer_id: 用户A的 ✅
```

### 场景2: 用户B配置了完整OAuth凭证并授权

```
用户B配置:
  - login_customer_id: 9876543210 ✅ (必填，已配置)
  - client_id: xxxx.apps.googleusercontent.com ✅
  - client_secret: GOCSPX-xxxx ✅
  - developer_token: yyyy ✅
  - refresh_token: 1//0zzzz ✅ (已完成OAuth授权)
  - customer_id: 从google_ads_accounts读取

实际使用:
  - client_id: 用户B的 ✅
  - client_secret: 用户B的 ✅
  - developer_token: 用户B的 ✅
  - refresh_token: 用户B的 ✅
  - login_customer_id: 用户B的 (9876543210) ✅
  - customer_id: 用户B的 ✅
```

### 场景3: 用户C配置了部分OAuth凭证（不完整）

```
用户C配置:
  - login_customer_id: 5555555555 ✅ (必填，已配置)
  - client_id: xxxx.apps.googleusercontent.com ✅
  - client_secret: NULL ❌ (缺少)
  - developer_token: yyyy ✅

实际使用 (回退到共享配置):
  - client_id: autoads的 ✅ (因为配置不完整)
  - client_secret: autoads的 ✅
  - developer_token: autoads的 ✅
  - refresh_token: autoads的 ✅
  - login_customer_id: 用户C的 (5555555555) ✅
  - customer_id: 用户C的 ✅
```

---

## 🔐 安全考虑

### 数据隔离

1. **login_customer_id**: 用户独立，决定了用户可以访问哪些MCC下的账户
2. **customer_id**: 用户独立，决定了用户实际操作哪个广告账户
3. **refresh_token**:
   - 共享模式：使用autoads的token，但通过login_customer_id限制访问范围
   - 独立模式：使用用户自己的token

### 权限边界

```
共享模式访问链:
  autoads refresh_token
    → 用户的 login_customer_id (MCC)
      → 用户的 customer_id (广告账户)
        → 用户数据

独立模式访问链:
  用户的 refresh_token
    → 用户的 login_customer_id (MCC)
      → 用户的 customer_id (广告账户)
        → 用户数据
```

---

## 📝 UI 文案更新

### settings 页面提示

```
Google Ads API 配置

必填项:
┌─────────────────────────────────────────────────────────────┐
│ Login Customer ID (MCC账户ID) *必填                         │
│ [____________________________________________]              │
│ 📝 您的MCC管理账户ID，用于访问您管理的广告账户               │
└─────────────────────────────────────────────────────────────┘

选填项 (不填则使用平台共享配置):
┌─────────────────────────────────────────────────────────────┐
│ Client ID                                                   │
│ [____________________________________________]              │
│ 📝 OAuth 2.0客户端ID，如不填写将使用平台统一配置             │
├─────────────────────────────────────────────────────────────┤
│ Client Secret                                               │
│ [____________________________________________]              │
│ 📝 OAuth 2.0客户端密钥，如不填写将使用平台统一配置           │
├─────────────────────────────────────────────────────────────┤
│ Developer Token                                             │
│ [____________________________________________]              │
│ 📝 Google Ads API开发者令牌，如不填写将使用平台统一配置      │
└─────────────────────────────────────────────────────────────┘

💡 提示：如果您不配置上述选填项，系统将使用平台共享的OAuth凭证。
        您只需要提供自己的MCC账户ID即可使用关键词搜索量功能。
```

---

## ✅ 实施检查清单

- [x] 修改 settings 页面 UI 和必填标记
- [x] 修改 keyword-planner.ts 配置读取逻辑
- [x] 修改 OAuth 流程支持混合模式
  - [x] oauth/start/route.ts - 根据用户配置决定使用哪个client_id
  - [x] oauth/callback/route.ts - 根据use_own_config标记使用正确的凭证
- [x] 添加 UI 提示文案（混合模式说明）
- [ ] 测试场景1：用户未配置OAuth，使用共享配置
- [ ] 测试场景2：用户配置完整OAuth，使用独立配置
- [ ] 测试场景3：用户配置不完整，回退共享配置
- [x] 更新文档
